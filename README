zorbage-gdal: code for loading gdal files into zorbage data structures for further processing

To learn more about Zorbage please visit https://github.com/bdezonia/zorbage

Installing on Ubuntu

Prerequisites:

Install gdal and support files on ubuntu

sudo add-apt-repository ppa:ubuntugis/ppa
sudo apt-get update
sudo apt-get install gdal-bin
sudo apt-get install libgdal-dev
sudo apt-get install libgdal-java

